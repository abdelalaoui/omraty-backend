package com.omraty.backend.entities.enums;

/** Catégorie d'un voyage du catalogue (GET /packages, filtrable via ?category=). */
public enum TripPackageCategory {
    TOURISME,
    OMRA,
    PELERINAGE,
    AFFAIRES
}
